aml_classifier/
│
├── requirements.txt          # dependencies
├── .env.example              # ANTHROPIC_API_KEY=sk-...
│
├── prompts/
│   ├── system_prompt_v1.txt  # version history
│   └── system_prompt_v2.txt  # current (active)
│
├── data/
│   └── synthetic_transactions.json   # generated by faker_generator.py
│
├── db/
│   ├── schema.sql            # table definitions
│   └── database.py           # SQLite connection + helpers
│
├── pipeline/
│   ├── faker_generator.py    # synthetic data generation
│   ├── classifier.py         # calls Claude API per transaction
│   └── batch_runner.py       # reads DB → classify → write results
│
├── outputs/
│   └── results.db            # SQLite file (gitignored)
│
├── tests/
│   └── test_scenarios.py     # the 3 canonical scenarios from the HTML
│
└── app.py                    # Streamlit dashboard (last step)
